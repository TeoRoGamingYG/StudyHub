<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet author="dev.team" id="20260327-create-grades-table">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="grades"/>
            </not>
        </preConditions>

        <createTable tableName="grades">
            <column name="id" type="BIGINT" defaultValueSequenceNext="seq_grades_id">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="student_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="course_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="grade" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="graded_by" type="BIGINT"/>
            <column name="graded_at" type="TIMESTAMP" defaultValueComputed="NOW()"/>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="grades"
                baseColumnNames="student_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_grades_student"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="grades"
                baseColumnNames="course_id"
                referencedTableName="courses"
                referencedColumnNames="id"
                constraintName="fk_grades_course"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="grades"
                baseColumnNames="graded_by"
                referencedTableName="teachers"
                referencedColumnNames="id"
                constraintName="fk_grades_professor"
                onDelete="SET NULL"/>
    </changeSet>

</databaseChangeLog>